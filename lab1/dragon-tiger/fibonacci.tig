let
   function fibonacci(n : int) : int =
     /* ... complete here ... */
     if n <= 0
     then 0
     else 
      if n < 3
      then n
      else fibonacci(n-1) + fibonacci(n-2)
in
   for i := 1 to 8 do
     (print_int(fibonacci(i)); print("\n"))
end
